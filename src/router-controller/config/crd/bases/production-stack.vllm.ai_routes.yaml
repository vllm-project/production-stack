---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: routes.production-stack.vllm.ai
spec:
  group: production-stack.vllm.ai
  names:
    kind: Route
    listKind: RouteList
    plural: routes
    singular: route
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Backend reference
      jsonPath: .spec.backendRef.name
      name: Backend
      type: string
    - description: Route path
      jsonPath: .spec.path
      name: Path
      type: string
    - description: API schema
      jsonPath: .spec.apiSchema
      name: API Schema
      type: string
    - description: Route weight
      jsonPath: .spec.weight
      name: Weight
      type: integer
    - description: Route active status
      jsonPath: .status.isActive
      name: Active
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Route is the Schema for the routes API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RouteSpec defines the desired state of Route
            properties:
              apiSchema:
                default: openai
                description: APISchema specifies which API schema is supported by
                  this route
                enum:
                - openai
                - anthropic
                - vllm
                type: string
              backendRef:
                description: BackendRef is a reference to the Backend resource
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  fieldPath:
                    description: |-
                      If referring to a piece of an object instead of an entire object, this string
                      should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                      For example, if the object reference is to a container within a pod, this would take on a value like:
                      "spec.containers{name}" (where "name" refers to the name of the container that triggered
                      the event) or if no container name is specified "spec.containers[2]" (container with
                      index 2 in this pod). This syntax is chosen only to have some well-defined way of
                      referencing a part of an object.
                    type: string
                  kind:
                    description: |-
                      Kind of the referent.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                    type: string
                  resourceVersion:
                    description: |-
                      Specific resourceVersion to which this reference is made, if any.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                    type: string
                  uid:
                    description: |-
                      UID of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              configMapRef:
                description: ConfigMapRef is a reference to the ConfigMap to create
                  with the dynamic config
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  fieldPath:
                    description: |-
                      If referring to a piece of an object instead of an entire object, this string
                      should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                      For example, if the object reference is to a container within a pod, this would take on a value like:
                      "spec.containers{name}" (where "name" refers to the name of the container that triggered
                      the event) or if no container name is specified "spec.containers[2]" (container with
                      index 2 in this pod). This syntax is chosen only to have some well-defined way of
                      referencing a part of an object.
                    type: string
                  kind:
                    description: |-
                      Kind of the referent.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                    type: string
                  resourceVersion:
                    description: |-
                      Specific resourceVersion to which this reference is made, if any.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                    type: string
                  uid:
                    description: |-
                      UID of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              headers:
                additionalProperties:
                  type: string
                description: Headers are additional headers to add to the request
                type: object
              path:
                description: Path is the URL path to match for this route
                type: string
              rateLimitPerMinute:
                description: RateLimitPerMinute is the maximum number of requests
                  allowed per minute
                format: int32
                minimum: 0
                type: integer
              secretRef:
                description: SecretRef is a reference to a secret containing API keys
                  for this route
                properties:
                  key:
                    description: Key is the key in the secret to use
                    type: string
                  name:
                    description: Name is the name of the secret
                    type: string
                  namespace:
                    description: Namespace is the namespace of the secret
                    type: string
                required:
                - key
                - name
                type: object
              semanticCachingConfig:
                description: SemanticCachingConfig provides configuration for semantic
                  caching
                properties:
                  maxCacheSize:
                    default: 1000
                    description: MaxCacheSize is the maximum number of entries in
                      the cache
                    format: int32
                    minimum: 1
                    type: integer
                  similarityThreshold:
                    default: 95
                    description: SimilarityThreshold is the threshold for semantic
                      similarity (0-100)
                    format: int32
                    maximum: 100
                    minimum: 0
                    type: integer
                  ttl:
                    default: 3600
                    description: TTL is the time-to-live for cached entries in seconds
                    format: int32
                    minimum: 1
                    type: integer
                type: object
              semanticCachingEnabled:
                default: false
                description: SemanticCachingEnabled indicates whether semantic caching
                  is enabled for this route
                type: boolean
              timeout:
                default: 300
                description: Timeout is the request timeout in seconds
                format: int32
                minimum: 1
                type: integer
              weight:
                default: 100
                description: Weight is the routing weight for this route (0-100)
                format: int32
                maximum: 100
                minimum: 0
                type: integer
            required:
            - apiSchema
            - backendRef
            - configMapRef
            - path
            type: object
          status:
            description: RouteStatus defines the observed state of Route
            properties:
              cacheHitRatePercent:
                description: CacheHitRate is the cache hit rate for this route (if
                  semantic caching is enabled)
                format: int32
                type: integer
              conditions:
                description: Conditions represent the latest available observations
                  of the Route's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              isActive:
                description: IsActive indicates whether the route is active
                type: boolean
              lastConfiguredTime:
                description: LastConfiguredTime is the last time the route was configured
                format: date-time
                type: string
              requestCount:
                description: RequestCount is the number of requests processed by this
                  route
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
